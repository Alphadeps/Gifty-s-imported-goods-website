/**
 * config.js
 * 
 * NOTE: This file is now deprecated and kept only for backward compatibility 
 * with existing HTML script tags. 
 * 
 * The system now loads configuration dynamically from the backend 
 * (Express API) to support environment variables in Vercel.
 * 
 * Check app.js (initAppConfig) and admin.js (initAdminConfig) for logic.
 */
window.CONFIG = window.CONFIG || {};
